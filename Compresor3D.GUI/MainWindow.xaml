<Window x:Class="Compresor3D.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compressor3D - Compresión Inteligente" 
        Height="600" Width="800"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0098FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#555555"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Compressor3D" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#007ACC"/>
            <TextBlock Text="Compresión inteligente con Context Mixing (PAQ1) y transformadas 3D" 
                       FontSize="12" 
                       Foreground="#AAAAAA" 
                       Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Método:" 
                       Foreground="White" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            
            <ComboBox x:Name="MethodSelector" 
                      Grid.Column="1" 
                      Style="{StaticResource DarkComboBox}"
                      SelectedIndex="0">
                <ComboBoxItem Content="Auto (recomendado)"/>
                <ComboBoxItem Content="PAQ1 (mejor para texto)"/>
                <ComboBoxItem Content="BWT+Zstd (mejor para imágenes)"/>
            </ComboBox>
            
            <Button x:Name="AddFilesBtn" 
                    Grid.Column="2" 
                    Content="Añadir archivos" 
                    Style="{StaticResource DarkButton}"
                    Margin="10,0,5,0"
                    Click="AddFilesBtn_Click"/>
            
            <Button x:Name="AddFolderBtn" 
                    Grid.Column="3" 
                    Content="Añadir carpeta" 
                    Style="{StaticResource DarkButton}"
                    Click="AddFolderBtn_Click"/>
        </Grid>
        
        <!-- Drop Zone / File List -->
        <Border Grid.Row="2" 
                BorderBrush="#3F3F46" 
                BorderThickness="2" 
                CornerRadius="8"
                Background="#252526"
                AllowDrop="True"
                DragDrop.DragEnter="DropZone_DragEnter"
                DragDrop.DragLeave="DropZone_DragLeave"
                DragDrop.Drop="DropZone_Drop">
            <Grid>
                <!-- Placeholder -->
                <TextBlock x:Name="DropPlaceholder"
                           Text="Arrastra archivos o carpetas aquí"
                           Foreground="#666666"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                
                <!-- File List -->
                <ListView x:Name="FileList" 
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="White"
                          Visibility="Collapsed">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Archivo" Width="350" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Tamaño" Width="100" DisplayMemberBinding="{Binding Size}"/>
                            <GridViewColumn Header="Estado" Width="150" DisplayMemberBinding="{Binding Status}"/>
                            <GridViewColumn Header="Ratio" Width="80" DisplayMemberBinding="{Binding Ratio}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
        
        <!-- Progress -->
        <StackPanel Grid.Row="3" Margin="0,15,0,15">
            <ProgressBar x:Name="CompressionProgress" 
                         Height="8" 
                         Background="#2D2D30" 
                         Foreground="#007ACC"
                         BorderThickness="0"
                         Visibility="Collapsed"/>
            <TextBlock x:Name="ProgressText" 
                       Text="" 
                       Foreground="#AAAAAA" 
                       FontSize="12" 
                       Margin="0,5,0,0"
                       Visibility="Collapsed"/>
        </StackPanel>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CompressBtn" 
                    Content="Comprimir" 
                    Style="{StaticResource DarkButton}"
                    Width="120"
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="CompressBtn_Click"/>
            <Button x:Name="DecompressBtn" 
                    Content="Descomprimir" 
                    Style="{StaticResource DarkButton}"
                    Width="140"
                    Click="DecompressBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
